{
  "name": "stockwatch",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "api", "worker", "web"],
  "scripts": {
    "test": "CI=true npm run test --workspaces --if-present && vitest run",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "typescript-eslint": "^8.3.0",
    "eslint": "^9.9.0",
    "eslint-plugin-import": "^2.29.1",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
